# M009 — Server-owned session container and claim commit

- Completed: 2026-09-23
- Result: The server Inventory feature now owns isolated session containers with fresh item identities, copied reads, guarded expiration, and a reserved `TakeFromContainer` commit. A claim rechecks both revisions, writes the player successor inside a Scribe transaction, then replaces the container source. Rollback leaves the source claimable; an exceptional post-write result is reconciled from the current player snapshot or quarantined if ownership cannot be proved. No client protocol or world container is present yet.
- Verification: `lute run scripts/verify.luau` passed with 32 native tests, lint, analysis, build profiles, and documentation regression. The guarded Studio suite passed 31 tests, including eight focused session transfer cases. A connected Studio server playtest of the actual transfer module with an isolated Scribe mock bundle accepted two claims, rejected a stale request, blocked observer reentry, and ended with both revisions at 2 and both items in the player snapshot; the task-owned play session and bundle were stopped. Live DataStore save and shutdown durability remain unverified.
