# M020 — Inventory mock persistence round trip

- Completed: 2026-09-24
- Result: A guarded Studio test uses the pinned Scribe 2.3.0 mock store and an isolated player lifecycle. It seeds a valid inventory, accepts a rotation through the production mutation path at revision 7→8, confirms a mock `Flush`, ends the session, reopens it, and reads the restored state through production readiness validation. Revision, item identities, definitions, placement, rotation, and item count match the accepted snapshot. The test uses Scribe's pinned internal server construction seam because the edit-mode schema bundle does not start a live server. The Studio diagnostic guard now supports exact expected raw output so ProfileStore's edit-mode API notice cannot hide unrelated diagnostics. Dependency preparation tolerates Wally's Windows line-ending normalization while preserving the checked-out lockfile bytes.
- Verification: Canonical `lute run scripts/verify.luau` passed 33 native tests, documentation checks, lint, Luau analysis, Rojo build, and development/release profiles. `lute run scripts/verify.luau --stage studio` passed all 38 guarded Studio tests, including the new round trip. This proves mock-store save and reload only; live DataStore durability, autosave, player leave, and shutdown remain unverified, so V7 remains open.
