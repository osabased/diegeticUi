# M005 — Client to server inventory mutation requests

- Completed: 2026-09-22
- Result: Blink now carries typed reposition and rotation requests. The server binds them during Inventory startup, uses Blink's sending Player as owner, and returns either the committed revision or a stable rejection reason. The client exposes small request methods; Scribe remains the inventory replication path.
- Verification: The canonical verifier passed with 26 native tests, and the Studio suite passed 18 tests. The focused protocol test checked sender forwarding, committed revision, stale rejection, and disconnect cleanup. A connected Studio client request reached the production server and returned `ItemNotPlaced` for its empty inventory. Studio command execution uses a separate module cache, so the command probe initialized its own client Inventory module before requesting. The task-owned play session was stopped. The Blink verifier now compiles a disposable schema copy after a tracked-output restore failure exposed a file-open race; the regenerated outputs passed the full verifier. Live DataStore durability and a successful network request against persisted player data remain unverified.
