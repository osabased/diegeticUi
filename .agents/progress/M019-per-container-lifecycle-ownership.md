# M019 — Per-container lifecycle ownership

- Completed: 2026-09-23
- Result: Each bound world container now owns its part connections through Janitor. Retirement closes access before destroying that owner and preserves deferred inventory disposal for an in-flight claim. A direct part-destruction test covers the additional lifecycle path. LemonSignal has no in-process event consumer here, and Charm has no shared application state to own beyond Scribe-backed inventory; adding either now would introduce an unused interface or a second state authority.
- Verification: The canonical verifier passed 33 native tests, analysis, and build profiles. The final guarded Studio suite passed 37 tests, including part removal, direct destruction, and expiration during a claim. A connected Studio server probe bound and inspected a part, destroyed it, then observed `ContainerMissing` with no new project errors; its task-owned play session was stopped. Two earlier full Studio runs passed all 37 assertions but failed the diagnostic guard on the known intermittent late callback warning; retained logs are `.verify/studio-diagnostics/1790198502-000.log` and `.verify/studio-diagnostics/1790198529-000.log`.
