--!strict
--!native
--!optimize 2
--file generated by blink 1.0.0-pre.8 (https://github.com/1Axen/blink)
--this file is not meant to be edited


local PLAYERS = game:GetService("Players")
local REPLICATED_STORAGE = game:GetService("ReplicatedStorage")
local RUN_SERVICE = game:GetService("RunService")

assert(RUN_SERVICE:IsServer(), "The server module can only be required from the server!")
local RELIABLE_EVENT = REPLICATED_STORAGE:FindFirstChild("f6c5caf92ed591640e752817a449d3b7") :: RemoteEvent
if not RELIABLE_EVENT then
	local event = Instance.new("RemoteEvent")
	event.Name = "f6c5caf92ed591640e752817a449d3b7"
	event.Parent = REPLICATED_STORAGE
	RELIABLE_EVENT = event
end
local UNRELIABLE_EVENT = REPLICATED_STORAGE:FindFirstChild("69bd090d3eb76814a1e57589e308774a") :: UnreliableRemoteEvent
if not UNRELIABLE_EVENT then
	local event = Instance.new("UnreliableRemoteEvent")
	event.Name = "69bd090d3eb76814a1e57589e308774a"
	event.Parent = REPLICATED_STORAGE
	UNRELIABLE_EVENT = event
end
type Save = {
	read buffers: { buffer },
	read lengths: { number },
	read unknown: { any },
}
local players_map: { [Player]: Save } = {}
const function create_save(player: Player)
	players_map[player] = {
		buffers = table.create<<buffer>>(16),
		lengths = table.create<<number>>(16),
		unknown = table.create<<any>>(16)
	}
end
for _, player in PLAYERS:GetPlayers() do
	create_save(player)
end
PLAYERS.PlayerAdded:Connect(function(player)
	create_save(player)
end)
PLAYERS.PlayerRemoving:Connect(function(player)
	players_map[player] = nil
end)

local in_buffer = buffer.create(64)
local in_cursor = 0
local in_unknown: { any } = table.create(16)
local in_unknown_cursor = 0

local out_buffer = buffer.create(1024)
local out_cursor = 0
local out_length = 1024
local out_unknown: { any } = table.create(64)

local function advance(bytes: number): number
	local cursor = in_cursor
	in_cursor = (cursor + bytes) :: number
	return cursor
end

local function allocate(bytes: number)
	local cursor = out_cursor
	local in_use: number = (cursor + bytes)

	if in_use > out_length then
		while in_use > out_length do
			out_length *= 1.5
		end

		local buf = buffer.create(out_length)
		buffer.copy(buf, 0, out_buffer, 0, cursor)
		out_buffer = buf
	end

	out_cursor = in_use
end

local queues = {}
local invocations = {}
local templates = {}
local invocations_bitset = 4294967295
local connections = {}
local types = {}
function types.TY_read_0x000001cd2b032c80(): (string) 
	-- bb1 {
	local r0 = buffer.readu32(in_buffer, advance(4))
	local r1 = buffer.readstring(in_buffer, advance(r0), r0)
	-- bb1 }
	return r1
end
function types.TY_write_0x000001cd2b032c80(p1: string): () 
	-- bb1 {
	local r0 = #p1
	local r1 = out_cursor
	allocate(4)
	buffer.writeu32(out_buffer, r1, r0)
	local r2 = out_cursor
	allocate(r0)
	buffer.writestring(out_buffer, r2, p1, r0)
	-- bb1 }
end
function types.TY_read_0x000001cd2b032bc0(): (boolean) 
	-- bb1 {
	local r0 = if buffer.readu8(in_buffer, advance(1)) == 1 then true else false
	-- bb1 }
	return r0
end
function types.TY_write_0x000001cd2b032bc0(p1: boolean): () 
	-- bb1 {
	-- bb1 }
	if typeof(p1) ~= 'boolean' then
		-- bb2 {
		error(string.format('Expected a boolean, got %s instead', typeof(p1)))
		-- bb2 }
		-- goto (bb3)
	end
	-- bb3 {
	local r0 = out_cursor
	allocate(1)
	buffer.writeu8(out_buffer, r0, if p1 == true then 1 else 0)
	-- bb3 }
end
function types.TY_read_0x000001cd2b032a70(): (string) 
	-- bb1 {
	local r0 = buffer.readu32(in_buffer, advance(4))
	local r1 = buffer.readstring(in_buffer, advance(r0), r0)
	-- bb1 }
	return r1
end
function types.TY_write_0x000001cd2b032a70(p1: string): () 
	-- bb1 {
	local r0 = #p1
	local r1 = out_cursor
	allocate(4)
	buffer.writeu32(out_buffer, r1, r0)
	local r2 = out_cursor
	allocate(r0)
	buffer.writestring(out_buffer, r2, p1, r0)
	-- bb1 }
end
function types.TY_read_0x000001cd2b0329e0(): (number) 
	-- bb1 {
	local r0 = buffer.readf32(in_buffer, advance(4))
	-- bb1 }
	return r0
end
function types.TY_write_0x000001cd2b0329e0(p1: number): () 
	-- bb1 {
	local r0 = out_cursor
	allocate(4)
	buffer.writef32(out_buffer, r0, p1)
	-- bb1 }
end
RELIABLE_EVENT.OnServerEvent:Connect(function(player: Player, packet_buffer: buffer, packet_unknowns: {unknown}): () 
	-- bb1 {
	local r0 = buffer.len(packet_buffer)
	in_buffer = packet_buffer
	in_cursor = 0
	in_unknown = packet_unknowns
	in_unknown_cursor = 0
	-- bb1 }
	while in_cursor < r0 and player.Parent ~= nil do
		-- bb2 {
		local r1 = buffer.readu8(in_buffer, advance(1))
		-- bb2 }
		-- if (1/2) (bb3, bb10)
		if r1 == 96 then
			-- bb3 {
			-- REMARK: RequestButtonAction
			local r2 = connections[96]
			local r3 = buffer.readu8(in_buffer, advance(1))
			local r4
			-- bb3 }
			r4 = types.TY_read_0x000001cd2b032c80()
			-- bb4 {
			-- bb4 }
			-- if (1/2) (bb5, bb6)
			if r2 == nil then
				-- bb5 {
				local r5 = out_cursor
				allocate(1)
				buffer.writeu8(out_buffer, r5, 96)
				local r6 = out_cursor
				allocate(1)
				buffer.writeu8(out_buffer, r6, r3)
				local r7 = out_cursor
				allocate(1)
				buffer.writeu8(out_buffer, r7, 0)
				-- bb5 }
				-- goto (bb9)
			-- bb6 {
			-- bb6 }
			-- if (2/2) (bb7, bb9)
			elseif r2 ~= nil then
				-- bb7 {
				local r5 = player
				task.spawn(function(): () 
					-- bb1 {
					local r6, r7, r8, r9 = pcall(r2, r5, r4)
					local r10 = players_map[r5]
					-- bb1 }
					if r10 == nil then
						-- bb2 {
						-- bb2 }
						return 
					end
					-- bb3 {
					out_cursor = 0
					table.clear(out_unknown)
					local r11 = out_cursor
					allocate(1)
					buffer.writeu8(out_buffer, r11, 96)
					local r12 = out_cursor
					allocate(1)
					buffer.writeu8(out_buffer, r12, r3)
					local r13 = out_cursor
					allocate(1)
					buffer.writeu8(out_buffer, r13, if r6 == true then 1 else 0)
					-- bb3 }
					if r6 == true then
						-- bb4 {
						-- bb4 }
						types.TY_write_0x000001cd2b032bc0(r7)
						-- bb5 {
						-- bb5 }
						types.TY_write_0x000001cd2b032a70(r8)
						-- bb6 {
						-- bb6 }
						types.TY_write_0x000001cd2b0329e0(r9)
						-- bb7 {
						-- bb7 }
						-- goto (bb8)
					end
					-- bb8 {
					local r14 = buffer.create(out_cursor)
					buffer.copy(r14, 0, out_buffer, 0, out_cursor)
					local r15 = r10.unknown
					table.insert(r10.buffers, r14)
					table.insert(r10.lengths, out_cursor)
					table.move(out_unknown, 1, #out_unknown, (#r15 + 1), r15)
					-- bb8 }
				end)
				-- bb7 }
				-- goto (bb9)
			end
			-- bb9 {
			-- bb9 }
			-- goto (bb13)
		-- bb10 {
		-- bb10 }
		-- if (2/2) (bb11, bb13)
		else			-- bb11 {
			error(string.format('Unknown event id: %d', r1))
			-- bb11 }
			-- goto (bb13)
		end
		-- bb13 {
		-- bb13 }
		-- goto (bb14)
	end
	-- bb14 {
	-- bb14 }
end)
UNRELIABLE_EVENT.OnServerEvent:Connect(function(player: Player, packet_buffer: buffer, packet_unknowns: {unknown}): () 
	-- bb1 {
	local r0 = buffer.len(packet_buffer)
	in_buffer = packet_buffer
	in_cursor = 0
	in_unknown = packet_unknowns
	in_unknown_cursor = 0
	-- bb1 }
	while in_cursor < r0 and player.Parent ~= nil do
		-- bb2 {
		local r1 = buffer.readu8(in_buffer, advance(1))
		-- bb2 }
		-- if (1/1) (bb3, bb5)
		if true == true then
			-- bb3 {
			error(string.format('Unknown event id: %d', r1))
			-- bb3 }
			-- goto (bb5)
		end
		-- bb5 {
		-- bb5 }
		-- goto (bb6)
	end
	-- bb6 {
	-- bb6 }
end)
RUN_SERVICE.Heartbeat:Connect(function(): () 
	-- bb1 {
	-- bb1 }
	for r0, r1 in players_map do
		-- bb2 {
		local r2 = r1.buffers
		-- bb2 }
		if #r2 > 0 then
			-- bb3 {
			local r3 = r1.lengths
			local r4 = r1.unknown
			local r5 = 0
			-- bb3 }
			for r6, r7 in r3 do
				-- bb4 {
				r5 = (r5 + r7)
				-- bb4 }
				-- goto (bb5)
			end
			-- bb5 {
			local r6 = 0
			local r7 = buffer.create(r5)
			-- bb5 }
			for r8, r9 in r2 do
				-- bb6 {
				buffer.copy(r7, r6, r9)
				r6 = (r6 + r3[r8])
				-- bb6 }
				-- goto (bb7)
			end
			-- bb7 {
			RELIABLE_EVENT:FireClient(r0, r7, r4)
			table.clear(r2)
			table.clear(r3)
			table.clear(r4)
			-- bb7 }
			-- goto (bb8)
		end
		-- bb8 {
		-- bb8 }
		-- goto (bb9)
	end
	-- bb9 {
	-- bb9 }
end)

return {
	StepReplication = function(): () 
		-- bb1 {
		-- bb1 }
		for r0, r1 in players_map do
			-- bb2 {
			local r2 = r1.buffers
			-- bb2 }
			if #r2 > 0 then
				-- bb3 {
				local r3 = r1.lengths
				local r4 = r1.unknown
				local r5 = 0
				-- bb3 }
				for r6, r7 in r3 do
					-- bb4 {
					r5 = (r5 + r7)
					-- bb4 }
					-- goto (bb5)
				end
				-- bb5 {
				local r6 = 0
				local r7 = buffer.create(r5)
				-- bb5 }
				for r8, r9 in r2 do
					-- bb6 {
					buffer.copy(r7, r6, r9)
					r6 = (r6 + r3[r8])
					-- bb6 }
					-- goto (bb7)
				end
				-- bb7 {
				RELIABLE_EVENT:FireClient(r0, r7, r4)
				table.clear(r2)
				table.clear(r3)
				table.clear(r4)
				-- bb7 }
				-- goto (bb8)
			end
			-- bb8 {
			-- bb8 }
			-- goto (bb9)
		end
		-- bb9 {
		-- bb9 }
	end,
	exports = {
		RequestButtonAction = {
			On = function(callback: (player: Player, action_id: string) -> (boolean, string, number)): (() -> ()) 
				-- bb1 {
				-- bb1 }
				if connections[96] ~= nil then
					-- bb2 {
					warn(debug.traceback('[Blink]: Function "RequestButtonAction" already has a callback, the current callback will be discarded.', 2))
					-- bb2 }
					-- goto (bb3)
				end
				-- bb3 {
				connections[96] = callback
				-- bb3 }
				return function(): () 
					-- bb1 {
					connections[96] = nil
					-- bb1 }
				end
			end,
		},
	}
}